{
  "name": "saqib29.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx -s tsc -p tsconfig.json",
    "build:watch": "npx -s tsc -p tsconfig.json --watch",
    "start": "npx -s serve ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}
